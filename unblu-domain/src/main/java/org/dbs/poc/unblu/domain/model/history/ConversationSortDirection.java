package org.dbs.poc.unblu.domain.model.history;

/**
 * Sens du tri appliqué au listing des conversations.
 */
public enum ConversationSortDirection {

    /** Tri croissant (A → Z, ancien → récent). */
    ASC,

    /** Tri décroissant (Z → A, récent → ancien). */
    DESC
}
